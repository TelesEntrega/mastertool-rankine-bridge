"""Diff semântico (variáveis adicionadas/removidas, blocos alterados) —
SEGUNDA ENTREGA (não implementado)."""

from __future__ import annotations

from mastertool_bridge.exceptions import NotImplementedPhaseError


def semantic_diff(*_args, **_kwargs):
    raise NotImplementedPhaseError(
        "Diff semântico planejado para a segunda entrega; use o diff textual "
        "(object_diff/project_diff) por enquanto.")
